[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "instachip"
version = "0.1.1"
description = "A Verilog Package Manager"
authors = [{name = "Rithvik Redrouthu", email = "rithvik.redrouthu@gmail.com"}]

[project.scripts]
your-command = "instachip.cli:main"
