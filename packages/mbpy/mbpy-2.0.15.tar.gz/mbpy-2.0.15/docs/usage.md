# Usage

To use mpip in a project:

```{eval-rst}
.. click:: mbpy.cli:cli
   :prog: mpip
   :show-nested:
```

For more detailed information on each command, use the `--help` option:

```console
$ mpip --help
$ mpip <command> --help
```
