version: 1.1
general:
  condition:
    description: No change
    default: non
  mode_conteneur_actif:
    description: No change
    default: non
    disabled:
      type: jinja
      jinja: '{% if activer_client_ldap == "non" %}{% else %}disabled{% endif %}'
  mode_conteneur_actif2:
    description: No change
    default: non
    disabled: true
