---
version: '1.1'
condition: "no"  # a condition
variable:
  description: a variable
  disabled:
    jinja: |
      {% if _.condition == "yes" %}
      true
      {% else %}
      false
      {% endif %}
