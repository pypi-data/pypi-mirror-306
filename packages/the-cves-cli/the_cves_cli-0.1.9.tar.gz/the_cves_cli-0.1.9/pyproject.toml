[project]
name = "the-cves-cli"
version = "0.1.9"
description = "The CVEs CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.5.2",
    "requests>=2.32.3",
    "typer>=0.12.5",
]

[project.scripts]
the-cves-cli = "the_cves_cli.main:app"
