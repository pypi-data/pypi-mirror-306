[build-system]
requires = [
    "setuptools >= 56.0.0",
    "setuptools_scm >= 6.0.1, <7",
]
build-backend = "setuptools.build_meta"

[tool.black]
target-version = ["py36"]
skip-string-normalization = true
