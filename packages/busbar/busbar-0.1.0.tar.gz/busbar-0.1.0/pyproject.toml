[tool.poetry]
name = "busbar"
version = "0.1.0"
description = "Busbar namespace package (empty)"
authors = ["Muselab <opensource@muselab.com>"]
license = "BSD3"
packages = [
    { include = "busbar" }
]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
