#!python
import os
import sys
from importlib.resources import files, as_file
with as_file(files('lalapps') / 'bin' / 'lalapps_string_contour_plotter_largeloops') as path:
    os.execv(path, sys.argv)
