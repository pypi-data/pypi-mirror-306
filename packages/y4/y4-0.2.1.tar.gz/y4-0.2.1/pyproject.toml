[project]
name = "y4"
version = "0.2.1"
dependencies = [
    "jq",
    "pyyaml",
]
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
y4 = "y4.__main__:main"
