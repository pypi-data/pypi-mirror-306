[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aigdb"
version = "0.1.0"
description = "A debugging tool for AI applications."
authors = [{ name = "Wei-Lin Chiang", email = "weichiang@berkeley.edu" }]
requires-python = ">=3.7"

