[project]
name = "metermetrics"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "svtter", email = "svtter@qq.com"},
]
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "seaborn>=0.13.2",
    "h5py>=3.12.1",
]
requires-python = "==3.9.*"
readme = "README.md"
license = {text = "not open"}


[tool.pdm]
distribution = true

[dependency-groups]
dev = ["devpk>=0.2.0"]
