[project]
name = "mmray"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openmm>=8.1.0",
    "ray[default]>=2.38.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
